package lekcja17.data.i.czas;

import java.time.DayOfWeek;

class DateAndTimeLesson {

    /**
     *  @formatter:off
     *
     * Data i czas:
     *  - LocalDate - now, of, parse, plusDays, minus (ChronoUnit)
     *  - LocalTime
     *  - LocalDateTime
     *  - DateTimeFormatter - ofPattern
     *  - DayOfWeek enum
     *  - ZonedDateTime vs LocalDateTime - ZoneId i ZoneOffset
     *  - Period - between, ofDays
     *  - Duration - between, ofSeconds
     *
     *  @formatter:on
     */
    public static void main(String[] args) {
        DayOfWeek a;
    }

}
